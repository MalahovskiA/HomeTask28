spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/market?currentSchema=market_place
    username: postgres
    password: root
  jpa:
    database: postgresql
    open-in-view: false
    show-sql: true
server:
  port: 8085